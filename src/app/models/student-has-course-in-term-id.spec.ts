import { StudentHasCourseInTermID } from './student-has-course-in-term-id';

describe('StudentHasCourseInTermID', () => {
  it('should create an instance', () => {
    expect(new StudentHasCourseInTermID()).toBeTruthy();
  });
});
